// Plaid removed — Pakistani developers cannot register for Plaid
export default function PlaidAccounts(_: { userId: string }) { return <div className="p-8 text-center text-surface-400">Bank Feeds (Plaid) is not available in your region.</div>; }
